# Caddy — обратный прокси с автоматическим Let's Encrypt SSL.
# Домен должен указывать A-записью на IP этого сервера, иначе сертификат
# не выпустится (Caddy будет повторять попытки).
iqsmeta.ru, www.iqsmeta.ru {
	reverse_proxy app:3000
}
